import { Container } from 'postcss'
import React from 'react'
import styles from '../style';

const Hero = () => {
  return (
    <section id="home" className={`flex ${styles.heading2}`}>

    </section>
  )
}

export default Hero